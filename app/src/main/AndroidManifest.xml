<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- برای ارتباط با سرویس AI -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- برای گرفتن عکس / ضبط ویدئو -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- برای ضبط صدای موتور -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- برای انتخاب عکس/ویدئو از گالری (اندروید 13+ از Photo Picker استفاده می‌کند و نیازی به این مجوز ندارد) -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
        از اندروید 11 (API 30) به بعد، برنامه‌ها به‌طور پیش‌فرض نمی‌توانند سایر برنامه‌های
        نصب‌شده (از جمله برنامه‌های دوربین) را ببینند مگر این‌که صراحتاً در <queries> اعلام شوند.
        بدون این بخش، حتی اگر گوشی چند برنامه دوربین نصب داشته باشد، سیستم ممکن است اعلام کند
        هیچ برنامه‌ای برای مدیریت درخواست وجود ندارد و باعث ActivityNotFoundException و کرش شود.
        این دقیقاً همان مشکلی است که با دوربین پیش‌فرض خراب/غیرفعال رخ می‌دهد.
    -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.media.action.VIDEO_CAPTURE" />
        </intent>
    </queries>

    <application
        android:name=".SmartMechanicApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartMechanicAI"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.SmartMechanicAI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FileProvider برای اشتراک‌گذاری امن فایل‌های عکس/ویدئو گرفته‌شده با دوربین -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
